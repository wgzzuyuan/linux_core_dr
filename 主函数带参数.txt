int argc, char **argv 用于运行时,把命令行参数传入主程序。

char **argv是什么用法啊？
argv[0] 什么意思啊？ 


argc -- 命令行参数 总个数，包括 可执行程序名。
argv[i] --  第 i 个参数。
argv[0] -- 可执行程序名。


相当于char* argv[]就是一个指针数组

char **argv 相当于指针数组吗？？？


C语言 main(int argc,char**argv) 
第二个参数argv是一个指向字符型的指针数组的指针，这个字符型指针数组的的每个指针都指向一个字符串

这句话应该怎么理解？？？？


char **argv是一个指向字符型的指针数组的指针，首先它du是一个指针，这个指针是指向数组的，这个数组里面的成员也指针，
成员指针是指向字符型数组的。char **argv也可写成char argv[][]， 看到数组就好理解了。
数组的各指针分别指向一个字符串。

例如运行：rec.exe  4  5.2
argc 等于 3， argv[0] 是 "rec.exe", argv[1] 是 "4", argv[2] 是 "5.2".



我认为自己还是没有真正的理解“指针和数组”之间的差异，差异是什么？？？
真正地尝试理解数组和指针之间的差异。


在某些情况下，将事情作为C中的函数参数没有意义。





































