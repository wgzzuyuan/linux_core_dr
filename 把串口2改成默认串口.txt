
1，


2，对串口0测试的方法，采用echo 方法对默认串口进行测试，怎么实现的？？？
# cat /dev/ttyS0 &
# echo testSerialCharString > /dev/ttyS0


3，


4，   为什么要 make menucnfig ？？？

配置设备，生成.config，让内核选择性编译，而不是全部编译，

因为内核中仅仅主控芯片就有很多厂家的主控芯片，每个主控芯片下又具体的配置的外设不一样，只编译你配置了的外设，你没有配置的外设就不编译了。


5，

1. 在bootloader 中把Linux 启动参数的串口输出改为别的串口

2，
#这个是要把串口0改为普通串口时要屏蔽内核的打印信息
2. 屏蔽内核解压时的打印信息


3， 把一个普通串口修改为调试串口的流程？？？
 (2)u-boot的打印串口
 (3)内核解压时的信息打印串口
 (4)内核调试串口
 (5)文件系统中使用的显示终端串口
 
 
 
 4，Uboot 中哪个文件和串口打印有关系？？？
 
 在BOOT的第二阶段初始化串口的控制台，
 
 5，Linux在进入BOOT前走的是芯片的ROM区域，然后才是BOOT，这个可以和STM32进行对比。
 
 Linux板子的ROM区域相当于STM32从“系统存储器”区域启动，从用户闪存指的是JTAG 下载程序。
 
 1）用户闪存 = 芯片内置的Flash。
 2）系统存储器 = 芯片内部一块特定的区域，芯片出厂时在这个区域预置了一段Bootloader，就是通常说的ISP程序。这个区域的内容在芯片出厂后没有人能够修改或擦除，即它是一个ROM区。
 
 到目前为止，为什么要把2个BOOT的引脚短接，我依然不知道原因。
 
 
 6，Linux怎么避免的这种问题？？？ 怎么避免程序起不来？？？ 怎么避免下载不进入？？？
 
 用分区的方式解决
 
 
 直接烧录固件
 
 
 7，
 
 
 
 
 
 

























































