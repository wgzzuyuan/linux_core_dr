1, 定时器用的双向链表，自己下来把双向链表用一下，以后就有实际操作经验了，自己要敲一遍




2，系统平台总线是怎么挂载的？？？


3，讯为老师：内核源码不是重点，工作中知道怎么用就可以了。

内核源码直接用就可以了，不影响我们用就可以了，直接调用就行了。



4，节点的关系    Kobject是最核心的，所有的驱动里面都有这个结构体

Kobject→→→bus→→→platform→→→I2C→→→SPI→→→USB
			                  →→→led→→→adc→→→buzzer（蜂鸣器）


5，
list_head把这个结构体搞明白了，剩下的就简单了，

list_head 就是传说中的那个双向链表


6，注册驱动，注册设备，------都是给链表里面添加一些东西


7，内核从哪里开始执行的？？？
	init/main.c→start_kernel
	start_kernel内核C语言运行的起点
	定时器初始化init_timers();











